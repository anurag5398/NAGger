# Set environment variable
## Environment variable PORT is undefined. Using port :8080 by default

# Setup local DB

# Create swagger docs
swag init -g main.go -o components/docs

# Setup log folder and config folder